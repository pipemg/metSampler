# simulation of data 
#
# 
# TABLE 1
names = expand.grid(sample(LETTERS,10),sample(letters,10),sample(LETTERS,10))
names = apply(names,1,paste,collapse="" )
names = sample(names,300)

mean = sample(200, replace = T)
sd = runif(200)

sample(1:100,300,replace=T)


# TATBLE 2

